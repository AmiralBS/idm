package fr.unice.polytech.idm.arduinoml.kernel.structural.value;

public enum EDirection implements EValue {
	LEFT, RIGHT, UP, DOWN;

	@Override
	public String toString() {
		return this.toString();
	}
}
